# 王への請願 (To Court the King) - P2P対戦WEB版 仕様書

## 概要
ボードゲーム「王への請願」（原題: Um Krone und Kragen / To Court the King）の
2人対戦（タイマン限定）P2P WEB版。GitHub Pagesにアップロードするだけで、
サーバーを別途用意せず動作する。

## 動作要件
- **完全にGitHub Pages単体で動作**（静的ファイルのみ、バックエンド不要）
- 接続には PeerJS の公開クラウド（無料・登録不要）を利用
  - `https://unpkg.com/peerjs@1.5.2/dist/peerjs.min.js` をCDN読み込み
- ブラウザの WebRTC DataChannel で実際のゲーム進行データをP2P直接送受信
- ホスト権威型（ホストの端末が正、ゲスト側はホストの判定に従う）

## ファイル構成
```
king-petition/
├── index.html          … 画面本体・メインゲーム進行ロジック
├── game.js             … カード20種の定義・取得条件判定ロジック
├── p2p.js              … PeerJS接続処理・ひらがな8文字ID生成
├── ui.js               … フェーズ表示・ルールモーダル・SE再生・トースト通知
├── style.css           … 見た目（中世宮廷風、深紅・金・濃紺基調）
└── assets/
    └── sounds/
        ├── dice_roll.mp3      … ダイスを振った時
        ├── dice_lock.mp3      … ダイスを確保/解除タップした時
        └── turn_change.mp3    … 手番が交代した時
```

## ゲームルール（確定仕様）

### 基本進行
1. 各プレイヤーは3個のダイスからスタート
2. 手番では、確定していないダイスを全て振る
3. 最低1個を確保（キープ）し、残りを再度振る…を繰り返す
4. 全ダイスを確保したら「確定」し、カード獲得フェーズへ
5. 確定した出目の組み合わせで取得条件を満たすカードを1枚取得（複数該当時は選択）
6. 手番終了、相手に交代

### カード一覧（20種、`game.js`の`CARDS`に定義済み）

| グループ | カード | 取得条件 | 効果 |
|---|---|---|---|
| 0 | 道化師 | どんな目でも可（未所持の場合のみ） | 未確定ダイス1個を振り直せる |
| 0 | ペテン師 | どんな目でも可（道化師所持時のみ、道化師と入れ替え） | 手番最初のダイスが1個増える |
| Ⅰ | 農夫 | ペア（同目2つ） | 手番最初のダイスが1個増える |
| Ⅰ | 下女 | 全て奇数 | 未確定ダイス1個に+1〜+3 |
| Ⅰ | 哲学者 | 全て偶数 | 2つのダイスの目を、和を変えず変更可 |
| Ⅰ | 職人 | 合計15以上 | 目1の未確定ダイスを追加 |
| Ⅰ | 衛兵 | スリーカード | 目2の未確定ダイスを追加 |
| Ⅱ | 狩人 | フォーカード | 目3の未確定ダイスを追加 |
| Ⅱ | 商人 | 合計20以上 | 好きな数の未確定ダイスを振り直し可 |
| Ⅱ | 天文学者 | ペア2組以上 | 未確定ダイス1個を確定済みと同じ目に変更 |
| Ⅲ | 女官 | ペア1組＋スリーカード1組 | 未確定ダイスに+1（6には不可） |
| Ⅲ | 質屋 | 合計30以上 | 目4の未確定ダイスを追加 |
| Ⅲ | 騎士 | ファイブカード | 目5の未確定ダイスを追加 |
| Ⅲ | 魔術師 | 12345または23456ストレート | 未確定ダイス1個を好きな目に変更 |
| Ⅳ | 錬金術師 | フルストレート(123456) | 3つのダイスの目を、和を変えず変更可 |
| Ⅳ | 司教 | ペア3組以上 | 目6の未確定ダイスを追加 |
| Ⅳ | 貴族 | スリーカード2組 | 未確定ダイスに+2（5には不可） |
| Ⅳ | 将軍 | シックスカード | 手番最初のダイスが2個増える |
| Ⅴ | 国王 | セブンカード | 取得した瞬間に勝利（タイマン限定のため即終了） |
| Ⅴ | 王妃 | 国王取得時に自動付与（単独取得不可） | 好きな目の未確定ダイスを追加できる |

**特記事項**: 同じ目が4つ揃った場合、「4つ組」と「ペア2組」の**両方**として扱う
（例: 6666は狩人にもcastronomer天文学者にも該当）。

### 「豚」（ケンさん用語：どのカード条件にも該当しない出目）の扱い
1. 通常カードの条件チェックを行い、1つでも該当すれば通常通り選択可能
2. 該当ゼロ（＝豚）の場合：
   - 道化師・ペテン師どちらも未所持なら → **道化師**が救済枠として取得可能
   - 道化師のみ所持なら → **ペテン師**が取得可能（道化師と入れ替わる）
   - 両方所持済みなら → 完全な空振り、何も取得できず手番終了

判定ロジックは `game.js` の `getClaimableCards(dice, ownedCardIds, availableCardIds)` に実装済み。

### 2人プレイ用カード枚数
| グループ | 枚数 |
|---|---|
| 0（道化師・ペテン師） | 無制限 |
| Ⅰ | 各2枚 |
| Ⅱ〜Ⅳ | 各1枚 |
| Ⅴ（国王・王妃） | 各1枚 |

`game.js` の `buildInitialDeck()` に実装済み。

### 効果の実装範囲（簡易実装であることの明記）
現状の実装は「ダイス+n個」系の効果を**次ターン以降の基本ダイス数に加算**する
簡易処理としている。「未確定ダイスの目を変更できる」系（下女・哲学者・天文学者・
魔術師・錬金術師・女官・貴族）は**カード定義上に効果テキストとして存在するが、
実際のダイス改変UIは未実装**。必要であれば追加実装が要る（後述の「今後の拡張候補」参照）。

## 接続の仕組み

### ひらがな8文字ルームコード
- `p2p.js` の `generateRoomId(8)` で、読み間違えにくい42音（濁点・小文字・「ん」除外）
  からランダムに8文字を生成
- 表示・共有用は常にひらがな。PeerJSに実際に渡すIDは、`hiraganaToPeerId()` で
  英数字に決定的変換したもの（例: `kpk_0001020304050607`）
  - PeerJSのPeer IDがマルチバイト文字を安全に扱える保証がないため、
    この変換層を設けている
- 参加側は表示されたひらがな8文字をそのまま入力すればよい（変換は自動）

### 通信方式
- ホストが `new Peer(internalId)` で部屋を開く
- ゲストが `peer.connect(internalHostId)` でホストに接続
- 以後は `DataConnection.send()` / `on("data")` でJSONメッセージを送受信
- メッセージ種別：
  - `JOIN`: ゲスト→ホスト、参加通知（名前を送る）
  - `SYNC`: ホスト→ゲスト、ゲーム状態全体の同期
  - `ACTION`: ゲスト→ホスト、操作要求（ROLL/TOGGLE_KEEP/CONFIRM/CLAIM）

### ホスト権威型の実装
- ゲスト側はUI操作をすると `ACTION` メッセージをホストに送るのみ
- ホスト側で実際の判定・乱数生成（ダイス目）を行い、結果を `SYNC` で全員に配信
- ゲスト側は受け取った `state` をそのまま描画するだけ（自分では判定しない）

## UI機能

### フェーズ表示（`ui.js` の `updatePhaseDisplay()`）
- rolling: ダイスを振ってください
- keeping: ダイスを確保してください
- claiming: カードを選択してください
- opponent_turn: 相手の手番です
- ended: ゲーム終了

### ルール参照モーダル
- ヘッダー横の「📖 ルール」ボタンでいつでも開閉可能
- `game.js` の `CARDS` 定義から自動生成されるため、カード内容を変更しても
  モーダル側の修正は不要

### 取得可能カードの自動ハイライト
- カード選択フェーズ中、確定ダイスに対して条件を満たすカードに
  緑枠のハイライト（`.card-item.claimable`）が自動的に付く
- `getClaimableCards()` をリアルタイムに呼び出して判定

### SE（効果音）
| タイミング | ファイル |
|---|---|
| ダイスを振った瞬間 | dice_roll.mp3 |
| ダイスをタップ（確保/解除）した瞬間 | dice_lock.mp3 |
| 手番が交代した瞬間 | turn_change.mp3 |

## デプロイ手順（GitHub Pages）
1. このディレクトリ一式をGitHubリポジトリにpush
2. リポジトリの Settings → Pages で、公開ブランチ・ディレクトリを設定
3. 発行されたURL（`https://<user>.github.io/<repo>/`）を開けば動作する
4. サーバー・ビルド工程は一切不要（静的ファイルそのまま配信でOK）

## 動作確認済み事項
- `game.js` のカード判定ロジックは Node.js 上で単体テスト済み
  （20種カード数、豚→道化師→ペテン師の遷移、各種役の判定、デッキ枚数）
- `p2p.js` のひらがな⇔内部ID変換は可逆性・文字種の安全性を確認済み
- HTML内の関数呼び出しとJS側の定義に不一致なし
- 使用しているCSSクラスは全てstyle.css内に定義あり

## 未検証・今後の拡張候補
- **実機でのPeerJSクラウド経由の実接続テストは未実施**（コードレベルの
  静的検証のみ。実際に2台の端末で部屋作成→参加→通信、という一連の
  動作は必ず実機で確認すること）
- 「未確定ダイスの目を変更できる」系カード効果のUI実装
  （現状は効果テキストの表示のみで、実際の操作フローは未実装）
- 効果の重複適用時の挙動（例: 農夫を2枚持った場合の挙動、2人戦では
  農夫は2枚まで存在しうる）
- 切断・再接続時のハンドリング（現状は簡易的な検知のみ）
- ホストが途中で閉じた場合のゲスト側の救済（現状、ホスト依存のため
  ホスト離脱でゲーム続行不可）
